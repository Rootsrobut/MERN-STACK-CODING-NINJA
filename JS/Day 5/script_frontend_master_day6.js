// ✅ READ: Accessing Elements

/*

// 1. By ID (returns a single element or null)
let el1= document.getElementById('myId');

// 2. By class name (returns an HTMLCollection)
let el2= document.getElementsByClassName('myClass');

// 3. Using CSS selectors - first match (returns an Element or null)
let el3= document.querySelector('.myClass');

// 4. All matches using CSS selectors (returns a NodeList)
let el4= document.querySelectorAll('.myClass');


// 🛠 UPDATE: Changing Content or Styles
let el = document.getElementById('elm');

// Change color
el.style.color = "red";

// Change text
el.innerText = "Hey there, how are you?";

// Add HTML inside element
el.innerHTML = "<strong>Hello!</strong>";

// Add attribute
el.setAttribute("title", "Hover text");


// 🧨 DELETE: Removing Elements
let el = document.getElementById('elm');
el.remove(); // Instantly removes it from the DOM

// ✨ CREATE: Adding New Elements
// 1. Create element
let p = document.createElement("p");

// 2. Add content
p.innerText = "Added via JS";

// 3. Append to existing DOM node
document.body.appendChild(p);


*/









